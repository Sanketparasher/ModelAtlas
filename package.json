{
  "name": "modelatlas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AI model recommendation MVP",
  "scripts": {
    "start": "npx http-server . -p 8000",
    "test": "node --test",
    "validate:benchmarks": "node scripts/validate-benchmarks.js",
    "update:benchmarks": "node scripts/update-benchmarks.js"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
